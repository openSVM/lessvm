#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn vm_creation() {
        let vm = VM::new();
        assert!(true); // Placeholder assertion
        // TODO: Add assertions to verify VM state after creation
    }
}