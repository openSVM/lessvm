//! Editor area component for the VS Code-like TUI

use tui::{
    backend::Backend,
    Frame
};

use crate::tui::app::App;

/// Render the editor area
pub fn render(_app: &App, _f: &mut Frame, _area: tui::layout::Rect) {
    
}